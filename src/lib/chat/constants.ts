export const CHAT_INITIAL_MESSAGE_LIMIT = 100;
export const CHAT_LOAD_OLDER_LIMIT = 50;
/** Load older messages when scroll is within the top 20% (after ~80% scroll-up). */
export const CHAT_SCROLL_LOAD_THRESHOLD = 0.2;
export const CHAT_LLM_CONTEXT_LIMIT = 100;
